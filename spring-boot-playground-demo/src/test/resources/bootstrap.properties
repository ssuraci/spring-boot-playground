embedded.postgresql.enabled = true
embedded.postgresql.reuseContainer = false
embedded.postgresql.dockerImage = postgres:13-alpine
embedded.postgresql.waitTimeoutInSeconds = 60
embedded.postgresql.database = spring_boot_demo_db
embedded.postgresql.user = postgres
embedded.postgresql.password = postgres
